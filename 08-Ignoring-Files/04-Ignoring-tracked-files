*Ignoring tracking future version of a tracked file:

1. Add a tracked file "tempfile2.txt" to the ".gitignore" file.

2. git rm --cached tempfile2.txt

3. git add .

4. git commit -m "Not going to track tempfile2.txt anymore"
